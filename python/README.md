# Optional Python Layer

Baseline workflow: **Excel → SQL → Power BI → Decision**.

Use Pandas + NumPy for EDA, coverage-distribution analysis, anomaly checks, repeatable transformations and scenario sensitivity where useful.

Suggested use here:
- Pandas: district coverage/rejection profiling.
- NumPy: percentile thresholds and cost/coverage sensitivity checks.

Python is optional; governed KPI logic stays in SQL and Power BI.
